{"name":"gateway_set_state","description":"Transition the session to a new workflow state. Capabilities with a non-empty `visible_in_states` list will only appear in tools/list when the session is in a matching state. Tools without `visible_in_states` are always visible. Returns the previous state, new state, and visible tool count.","parameters":{"properties":{"state":{"description":"Target workflow state name (e.g. \"checkout\", \"payment\", \"default\")","type":"string"}},"required":["state"],"type":"object"}}